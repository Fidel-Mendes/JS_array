const nomes = ['ana','ju','leo','paula'];

nomes.forEach(imprimeNomes);
//forEache pode receber até três funções, caso for necessario
function imprimeNomes (nome, indice){
    console.log(nome);
}
//forEach só pode receber funções, se não ele não ira funcionar

